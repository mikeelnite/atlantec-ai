# Web App

This folder contains the separate React + Vite web app for Help Your Gaeltacht.

## Run locally

1. Install dependencies:

```bash
npm install
```

2. Create a local env file from `.env.example` and fill in your own values.

3. Start the dev server:

```bash
npm run dev
```

4. Build for production:

```bash
npm run build
```
